#!/bin/sh

#
# DASICS tests are selected by the rootfs build profile and are run
# explicitly from the serial console.  Do not auto-run legacy user-space
# tests here: their output otherwise contaminates kernel/LKM regressions.
#
echo "DASICS rootfs ready; load the selected profile explicitly"
