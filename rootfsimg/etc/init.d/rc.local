#!/bin/sh

if grep -qw "dasics_test=m4_alpha" /proc/cmdline 2>/dev/null; then
    if [ -x /etc/init.d/rc.m4-alpha ]; then
        exec /etc/init.d/rc.m4-alpha
    fi
    echo "[M4-ALPHA] MISSING: /etc/init.d/rc.m4-alpha"
    echo "[M4-ALPHA] FAIL(total_fail=1)"
    exit 0
fi

exit 0
